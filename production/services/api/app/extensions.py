# =============================================================================
# app/extensions.py — Shared extension instances (initialised via app factory)
# =============================================================================
from flask_pymongo import PyMongo

mongo = PyMongo()
